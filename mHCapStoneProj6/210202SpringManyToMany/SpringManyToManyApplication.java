package SpringManyToMany;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SpringManyToManyApplication {

	public static void main(String[] args) {
		SpringApplication.run(SpringManyToManyApplication.class, args);
	}

}

/*
Created new sql user, or drop sql table to study and learn.
Run some test, which will create new roles.

Shown that JpaRepository, which internal handle all the CRUD.
*/